# macOS
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files (keep examples)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# Node / Next.js
node_modules/
web/.next/
out/
dist/
coverage/
*.tsbuildinfo
# Generated by `next typegen`, `next dev` and `next build`, and rewritten with a
# different route-types path by each — tracking it means an unstaged change
# after every switch between dev and build. `npm run typecheck` regenerates it.
next-env.d.ts

# IDE
.idea/
.vscode/


# Agent worktrees (transient)
.claude/worktrees/

# Working artifacts, not part of the product.
# The repository holds the app; planning and process docs stay local.
*.md
!README.md
tasks/
.claude/
.github/pull_request_template.md
.vercel
